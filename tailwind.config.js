const konstaConfig = require("konsta/config");

module.exports = konstaConfig({
  content: ["./src/**/*.{js,jsx}"],
  darkMode: "media",
  theme: {
    extend: {},
  },
  variants: {
    extend: {},
  },
  plugins: [],
});
